## Accessing This Repository

This is a reference repository for the Playwright training batch.

### For Participants
1. Fork this repository to your GitHub account
2. Clone your fork locally
3. Install dependencies using `npm install`
4. Run tests using `npx playwright test`

Do not push changes directly to the main repository.
