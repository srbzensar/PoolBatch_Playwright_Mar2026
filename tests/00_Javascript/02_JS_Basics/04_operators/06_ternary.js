let age = 18;
let message = (age >= 18) ? "Eligible to vote" : "Not eligible";  // Ternary operator example   
// If age is 18 or more, message will be "Eligible to vote", otherwise "Not eligible"
console.log(message);